{
  "name": "universal-template",
  "version": "1.0.0",
  "description": "Universal Template with AI-driven workflow",
  "scripts": {
    "setup": "pnpm install && lefthook install",
    "lint": "prettier --check .",
    "format": "prettier --write .",
    "test": "echo 'No test script defined yet'"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "@google/generative-ai": "^0.24.1",
    "lefthook": "^1.6.0",
    "prettier": "^3.2.0"
  }
}
